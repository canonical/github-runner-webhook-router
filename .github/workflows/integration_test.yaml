name: Integration tests

on:
  pull_request:

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@main
    secrets: inherit
    with:
      juju-channel: 3.1/stable
      channel: 1.28-strict/stable
      trivy-image-config: "trivy.yaml"
      self-hosted-runner: false
      rockcraft-channel: latest/edge
      charmcraft-channel: latest/edge
      # we use https://github.com/apps/cbartz-runner-test-app to test github app auth authentication
      extra-arguments: --github-app-id 945427 --github-app-installation-id 58159077 --webhook-test-repository cbartz-org/gh-runner-test
